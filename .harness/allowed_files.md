# 当前 Harness 写入边界

当前无 active OpenSpec change。`harden-grounded-citation-instruction` 已归档为
`2026-06-22-harden-grounded-citation-instruction`。

Exception context：`add-live-model-provider-eval` 在独立分支暂停；本 remediation 完成归档、
合并和推送后才能恢复。

## 当前允许修改

- 无 active stage 写入范围。
- 恢复 `add-live-model-provider-eval` 前必须切换到其独立分支并重新同步本文件。

## 禁止修改 / 禁止行为

- 不在已归档 remediation 上继续修改 runtime、tests 或长期 specs。
- 不在当前分支修改 paused live evaluator、fixture、rubric、profile 或旧失败报告。
- 不创建或规划 V24。
