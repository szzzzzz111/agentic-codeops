# 当前 Harness 写入边界

当前无 active OpenSpec change。`harden-model-provider-contract` 已完成、review、验证并归档为
`2026-06-22-harden-model-provider-contract`，等待集成到 `main`。

## 当前允许修改

- 无 active stage 写入范围。
- 下一项工作开始前必须创建独立 OpenSpec change 并重新同步本文件。

## 禁止修改 / 禁止行为

- 不在已归档 change 上继续修改 runtime、tests 或长期 specs。
- 不创建或规划 V24。
- `add-live-model-provider-eval` 必须作为后续独立 OpenSpec change。
- 新工作开始前必须创建独立 OpenSpec change 并重新同步本文件。
