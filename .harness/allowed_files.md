# 当前 Harness 写入边界

当前无 active development stage。V22 Worktree Re-verification 已实现、review 并归档。

开始下一阶段前，必须先创建对应 OpenSpec change，并重新同步本文件与
`.harness/review_checklist.md`。

## 当前允许修改

- V22 archive / merge / push 所需 durable docs 与 handoff closeout 文件。

## 禁止修改 / 禁止行为

- 不在未规划新阶段时修改 runtime 或 tests。
- 不恢复旧 `specs/00x-*` 作为规格入口。
- 不把 OpenSpec、Superpowers、MCP、plugin 或 `.codex/skills/**` 写成 RepoPilot runtime 能力。
- 不新增 `/chat` 顶层字段或公开 API。
