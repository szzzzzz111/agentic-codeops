# Tasks

## 1. Freeze And Plan

- [x] Freeze the original docs-only work on a clean worktree; this initial low/implement contract is superseded by
  section 5 and later authority epochs after the delivery-gate blocker was discovered.
- [x] Complete the original docs-only internal plan review; it does not satisfy the later authority-sensitive increment.

## 2. TDD

- [x] Add failing scanner cases for volatile tracked handoff claims, FEATURE_LIST stage narration and incomplete specs index.
- [x] Replace history-token docs assertions with current-first architecture and module-map assertions.

## 3. Documentation And Workflow

- [x] Add concrete risk routing and escalation fields to Harness rules/template.
- [x] Rewrite HANDOFF as a stable live-resume protocol.
- [x] Rewrite ARCHITECTURE as a current-first runtime document with code mapping.
- [x] Compress PROGRESS into durable status, debt, candidate sequence and archive index.
- [x] Repair FEATURE_LIST and OpenSpec index/archive guidance.

## 4. Verification And Review

- [x] Make targeted tests and repository scanner pass.
- [x] Run JSON parse, changed Python Ruff, authority/allowlist and diff checks.
- [x] Run active-change OpenSpec strict validation; `repair-documentation-information-architecture` is valid.
- [x] Run full-repository OpenSpec strict validation; `25 passed, 0 failed` after the user-authorized Purpose-only
  correction to the baseline `verified-patch-promotion` spec.
- [x] Run canonical repository verification because scanner/tests changed (`975 passed`; all required checks green).
- [x] After the later Purpose-only correction, rerun only the risk-matched OpenSpec, stage-doc, authority and diff gates;
  no runtime, Python or test file changed in that correction.
- [x] Complete internal implementation review and report residual risk without commit/push/archive.

## 5. Authority-Sensitive Delivery Gate Repair

- [x] Record the archive blocker and obtain direct user authorization for the recommended validator repair.
- [x] Freeze a high-risk authority epoch with push ceiling, exact validator/test/review/archive paths and unchanged remote tip.
- [x] Complete internal plan review plus two independent empty-context plan-review slots against one remediated packet.
- [x] Add RED cases for authority-bound low+zero success; unbound/high+zero rejection; non-int/negative counts;
  zero/non-empty receipts or history; and archive/commit packet binding.
- [x] Add RED cases for exact positive bound-count mismatches in plan/implementation, phase substitution, malformed
  binding schema, and legacy-positive compatibility without an authority-bound claim.
- [x] Add RED cases proving a bound future record's `implement` gate consumes the canonical plan review set and rejects
  missing input, non-canonical path, wrong phase/packet, incomplete evidence and implementation-set substitution.
- [x] Implement optional authority-scope slot-count binding plus strict non-negative integer/empty-history zero semantics
  plus a mandatory future-bound-stage `implement` plan review-set authority input, without weakening legacy positive-slot
  checks or retroactively validating this introducing stage's epoch-5 plan.
- [x] Make zero-slot reporting mark reviewer dispatch provenance not applicable while retaining activation/authority external
  evidence; keep positive-slot host dispatch provenance required.
- [x] Wire bound plan-review flags into the canonical Codex/OpenCode apply instructions and shared test-command contract.
- [x] Reject Chinese/English HANDOFF live-state assignments with or without `current`/`当前`, including half/full-width
  colons and Markdown list/emphasis/inline-code wrappers, while allowing stable bilingual live-query guidance, with
  positive and negative regressions.
- [x] Reject medium/high authority when either bound review phase is zero, independent of the current action phase.
- [x] Align canonical rules so positive slots require host dispatch provenance while authority-bound zero slots mark it
  `NOT_APPLICABLE` and retain activation-sequence checks.
- [ ] Complete two independent final implementation-review slots; post-finding focused checks and canonical verification
  passed (`1052 passed`, Ruff, stage-doc and skill-eval green).
- [ ] Preflight and sync/archive the delta spec, rebuild the post-archive packet, refresh both final slots, create the
  finite candidate, ff merge and exact-old-OID lease push.
