# Tasks

## 1. Freeze And Review

- [x] Reconcile live origin/main and create a clean isolated worktree from the exact authorized tip.
- [x] Freeze the real event entry, clean fixture, Git snapshot, completion claim, verification receipt, six faults,
  acceptance boundary and stop conditions.
- [x] Complete one internal narrow plan review and pass authority-bound zero-slot implement preflight.

## 2. TDD Validator

- [x] Add RED tests for the six deterministic failure scenarios.
- [x] Implement a pure observation validator that does not launch or mutate an Agent/worktree.
- [x] Make the positive same-snapshot observation and all fail-closed tests pass.

## 3. Real Qualification

- [x] Create a clean temporary Git fixture and record its baseline snapshot.
- [x] Run real Codex CLI in the fixture and capture its actual JSONL terminal/completion events.
- [x] Capture the completion snapshot, run an independent deterministic verifier, and capture the post-verification snapshot.
- [x] Validate and save the minimal real observation/report without representing synthetic test data as real evidence.

## 4. Verify And Report

- [x] Run focused tests, changed-file Ruff, OpenSpec, authority/allowlist and diff checks.
- [x] Run canonical repository verification because Python/test files changed.
- [x] Update durable progress/handoff with the bounded result, unknowns and stop/next-step decision.
- [x] Confirm the stage remains unarchived, uncommitted, unmerged and unpushed.
