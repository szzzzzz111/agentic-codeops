# OpenSpec Changes

本目录保存进行中的 OpenSpec changes。每个 change 应使用 kebab-case 命名，并包含 OpenSpec CLI 生成的 proposal、design、tasks 和 spec delta。

历史 `specs/00x-*` 已迁移并退役；当前长期规格入口是 `openspec/specs/`。active change 只保存本阶段 proposal、design、tasks 和 spec delta，完成归档后再同步长期规格。
